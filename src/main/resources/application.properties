spring.application.name=msvc-gateway-server

server.port=8080
eureka.client.service-url.defaultZone=http://localhost:8761/eureka

gateway.cors.allowed-origins=http://127.0.0.1:*,http://localhost:*

# Perfil dev (opcional): descomentar predicados/filtros de práctica en application.yml